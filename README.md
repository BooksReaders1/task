# 任务管理系统

一个基于浏览器 IndexedDB 的轻量级任务管理 Web 应用，专为追踪和管理作品任务设计。支持任务增删改查、状态分类、收藏功能、搜索筛选以及统计面板，集成 JM 配置和预览图功能。

## ✨ 特性

### 核心功能
- **任务管理**：创建、编辑、删除任务，支持批量添加（自动提取多个作品 ID）
- **状态分类**：三种状态 - 未使用、已完成、已失效
- **收藏功能**：标记喜欢的任务，快速筛选
- **搜索筛选**：实时搜索任务和备注内容
- **统计面板**：直观显示各类任务数量统计
- **数据持久化**：使用 IndexedDB 本地存储，无需服务器

### 高级功能
- **智能跳转**：根据作品 ID 位数自动选择跳转目标
  - 5-8 位：跳转到 JM 作品页
  - 9 位及以上：跳转到 P 站并复制作品 ID
- **JM 配置**：自定义 JM 跳转地址和预览图 API（通过左侧菜单访问）
- **预览图**：自动提取任务中的作品 ID 生成预览图
- **剪贴板粘贴**：一键从剪贴板读取并创建任务
- **数据导出/导入**：支持将全部任务数据导出为 JSON 文件用于备份迁移；提供两种导出方式：「导出数据 (文件)」同时复制并下载文件，「导出数据 (粘贴板)」仅复制到剪贴板不生成文件；导入时仅允许在空数据库中操作，支持文件导入和粘贴 JSON 文本两种方式
- **侧边菜单**：点击左上角三横线按钮展开黑色小菜单，集中管理配置和导入导出功能
- **响应式设计**：完美适配桌面端和移动端

## 🚀 快速开始

### 使用方法
1. 直接在浏览器中打开 `index.html` 文件
2. 无需安装任何依赖，无需服务器
3. 数据自动保存在浏览器本地

### 创建任务
1. 在输入框中输入任务文本（可包含作品 ID）
2. （可选）填写备注信息
3. 点击「保存任务」按钮
4. 或使用「粘贴」按钮从剪贴板快速导入

### 配置 JM
1. 点击左上角「☰」菜单按钮展开侧边栏
2. 点击「⚙️ 配置 JM」或「🔄 恢复默认 JM/预览」
3. 选择配置类型：
   - **JM 跳转地址**：设置 JM 网站的基础 URL
   - **预览图 API**：设置预览图服务的 API 地址
4. 输入新的 URL 并确认

## 📋 使用说明

### 任务操作
| 操作 | 说明 |
|------|------|
| ✏️ 编辑 | 修改任务文本和备注 |
| 💾 保存 | 保存编辑后的任务 |
| 🗑️ 删除 | 删除当前任务 |
| ⏳ 失效 | 将任务标记为已失效 |
| ✅ 完成 | 将任务标记为已完成 |
| ↩️ 取消 | 取消完成/失效状态 |
| ❤️ 喜欢 | 收藏任务 |
| 💔 取消喜欢 | 取消收藏 |
| 🖼️ 预览 | 查看作品预览图 |
| 📖 本地预览 | 仅 JM 数据显示，在新页面打开本地预览（5-8 位 ID） |
| 🤖 智能跳转 | 根据 ID 位数自动选择跳转 |
| 📘 跳转到 JM | 直接跳转到 JM 作品页 |
| 🖼️ 跳转到 P 站 | 跳转到 P 站并复制 ID |

### 筛选功能
- **全部任务**：显示所有任务
- **喜欢**：仅显示收藏的任务
- **未完成**：状态为「未使用」的任务
- **已完成**：状态为「已完成」的任务
- **已失效**：状态为「已失效」的任务

### 搜索技巧
- 在搜索框输入关键词，实时过滤任务和备注
- 点击「🧹 清空条件」清除搜索条件

## 🔧 技术细节

### 数据存储
- 使用 **IndexedDB** 进行本地数据持久化
- 数据库名称：`taskDB`
- 数据表：
  - `tasks`：存储任务信息（ID、文本、备注、状态、收藏标记、创建时间、预览图 URL）
  - `configs`：存储全局配置（JM 跳转地址、预览图 API）

### 作品 ID 提取
- 优先匹配连续的 9 位数字
- 若无 9 位数字，则提取所有数字（最多 9 位）
- 支持从任务文本中提取多个作品 ID 批量创建任务

### 预览图生成
- 自动从任务文本中提取作品 ID
- 根据配置的预览图 API 生成图片 URL
- 格式：`{API_BASE}{作品_ID}.jpg?u={时间戳}`
- 首次加载失败时可手动刷新

### 跳转逻辑
```javascript
// 智能跳转规则
if (作品 ID 长度 >= 5 && <= 8) {
    // 跳转到 JM: {JM_BASE}{作品_ID}/
} else if (作品 ID 长度 >= 9) {
    // 跳转到 P 站主页，并复制作品 ID 到剪贴板
}
```

## 🛠️ 开发说明

### 项目结构
```
/workspace
├── index.html      # 主页面（包含所有 HTML、CSS、JavaScript）
└── README.md       # 项目文档
```

### 代码特点
- **零依赖**：纯 HTML/CSS/JavaScript 实现
- **单文件**：所有代码在一个 HTML 文件中
- **响应式**：支持移动设备和桌面端
- **异步处理**：使用 Promise 处理 IndexedDB 操作

### 自定义配置
默认配置：
- JM 跳转地址：`https://jm18c-uoi.net/album/`
- 预览图 API：`https://cdn-msp.jm18c-uoi.net/media/albums/`

可通过界面配置按钮修改，配置保存在 IndexedDB 中。

## 📱 浏览器兼容性

- ✅ Chrome / Edge（推荐）
- ✅ Firefox
- ✅ Safari
- ✅ 移动端浏览器

> **注意**：部分功能（如剪贴板读写）需要在 HTTPS 或 localhost 环境下使用

## 📝 使用场景

1. **作品追踪**：记录已购买/想购买的作品
2. **进度管理**：标记作品的阅读/下载状态
3. **快速访问**：一键跳转到作品页面
4. **收藏整理**：管理喜欢的作品列表

## ⚠️ 注意事项

- 数据仅保存在本地浏览器，清除浏览器数据会导致数据丢失
- 建议定期备份重要任务信息
- 预览图加载依赖外部 API，需确保网络畅通
- 弹出窗口被拦截时，链接会自动复制到剪贴板

## 📄 许可证

本项目为开源项目，可自由使用和修改。

---

**享受便捷的任务管理体验！** 🎉

## 📝 更新日志

============= 2026-04-17 10:00:00 : **Add local preview button for JM tasks** ==========

<center>新增「本地预览」按钮：在任务卡片的操作列中增加📖本地预览按钮，仅当数据属于 JM（作品 ID 为 5-8 位）时显示；点击后打开新页面跳转到 https://booksreaders1.github.io/TestPicture/?id=XXX，其中 ID 为从任务中提取的数字；复用源代码中已有的 getSourceType() 和 extractNumber() 函数进行判断和提取</center>

========== ==== commit end =================

============= 2026-04-17 09:21:49 : **Add export to clipboard functionality** ==========

<center>新增导出（复制到粘贴板）功能：在左侧菜单中新增「📤 导出数据 (粘贴板)」选项，仅将数据复制到剪贴板而不生成文件；原有的「📤 导出数据」重命名为「📤 导出数据 (文件)」，保留复制并下载文件的完整功能；用户可根据需求选择两种不同的导出方式</center>

========== ==== commit end =================

============= 2026-04-17 09:17:05 : **Add JSON text paste import functionality** ==========

<center>新增导入数据支持粘贴 JSON 文本：在左侧菜单中新增「📥 导入数据 (粘贴)」选项，用户可以直接粘贴 JSON 格式的备份数据进行导入；同时保留原有的文件导入方式，现显示为「📥 导入数据 (文件)」；两种方式均仅允许在空数据库中操作，防止数据覆盖丢失</center>

========== ==== commit end =================

============= 2026-04-17 09:05:04 : **Add side menu UI and clipboard copy on export** ==========

<center>新增左侧滑出式菜单：将配置 JM、恢复默认 JM、恢复默认预览、导入导出按钮移入左侧黑色小菜单，点击左上角三横线按钮展开/收起；导出时自动复制数据到剪贴板</center>

========== ==== commit end =================

============= 2026-04-17 08:43:00 : **Add data export and import functionality for migration** ==========

<center>新增数据导出和导入功能：支持将全部任务数据导出为 JSON 格式文件用于备份迁移；导入时进行严格校验，仅当数据库为空时才允许导入，防止数据覆盖丢失</center>

========== ==== commit end =================

============= 2026-04-13 08:15:00 commit current : **Add graceful degradation for preview images** ==========

<center>为预览图弹窗添加错误处理机制：图片加载失败时自动显示友好的 SVG 占位图，避免显示浏览器默认的“破图”图标，提升用户体验</center>

========== ==== commit end =================

============= 2026-04-13 07:48:06 : **Fix README log logic and update view toggle UI** ==========

<center>修正 README 更新日志逻辑：新增记录而非修改，使用真实系统时间；优化视图切换按钮至卡片上方独立行，简化文字为 Emoji+ 名称</center>

========== ==== commit end =================

============= 2026-04-13 07:35:40 : **Fix README log logic and update view toggle UI** ==========

<center>修正 README 更新日志逻辑：新增记录而非修改，使用真实系统时间；优化视图切换按钮至卡片上方独立行，简化文字为 Emoji+ 名称</center>

========== ==== commit end =================

============= 2026-04-13 12:00:00 : **Redesign view switch button with simplified UI** ==========

<center>重构视图切换区域：按钮独立成行位于统计卡片上方，采用简洁样式仅显示 Emoji+ 视图名称（🔄 状态视图/🌐 来源视图），优化移动端响应式适配</center>

========== ==== commit end =================

============= 2026-04-13 05:52:03 : **Fix JM and PIXIV detection logic to match smart jump behavior** ==========

<center>修复 getSourceType 函数逻辑，使其与智能跳转判断条件完全一致（基于数字位数：5-8 位为 JM，9 位及以上为 PIXIV）</center>

========== ==== commit end =================

============= 2026-04-13 05:48:46 : **Add source-based filtering cards with view switcher** ==========

<center>新增来源视图切换功能，支持按"全部/JM/PIXIV/其他"筛选任务，统计卡片与智能跳转逻辑保持一致</center>

========== ==== commit end =================

============= 2026-04-13 05:48:46 : **Refactor task card UI with improved layout and action menu** ==========

<center>重构任务卡片 UI，采用改良版方案一：常驻预览 + 跳转按钮，收藏移至标题旁，状态标签可点击切换，其他操作收纳至更多菜单</center>

========== ==== commit end =================

============= 2026-04-13 05:48:46 : **Replace alert/confirm/prompt with non-blocking Toast and custom dialogs** ==========

<center>替换所有原生弹窗为页面顶部悬浮 Toast 通知和自定义对话框，避免硬打断用户体验</center>

========== ==== commit end =================

============= 2026-04-13 05:48:46 : **Add HTML escaping to prevent XSS vulnerabilities** ==========

<center>添加 HTML 转义函数，对任务文本、备注、状态字段和预览图 URL 进行安全处理，防止跨站脚本攻击</center>

========== ==== commit end =================

============= 2026-04-13 05:48:46 : **Initial commit: Task management system based on IndexedDB** ==========

<center>初始版本：基于 IndexedDB 的任务管理系统，支持增删改查、状态分类、收藏、搜索筛选、统计面板、JM 配置和预览图功能</center>

========== ==== commit end =================
